
---
layout: home
title: Home
---

<div class="parallax full-width">
  <style>
    header {
      z-index: 2;
      position: relative;
    }

    /* --- Stagger Animations --- */
    .home-content section {
      opacity: 0;
      transform: translateY(40px);
      transition: opacity 0.8s ease-out, transform 0.8s ease-out;
    }

    .home-content.visible section {
      opacity: 1;
      transform: translateY(0);
    }

    .home-content:nth-of-type(odd) section {
      transition-delay: 0.1s;
    }

    .home-content:nth-of-type(even) section {
      transition-delay: 0.3s;
    }

    /* Image hover and overlay */
    .image-overlay-container {
      position: relative;
      display: inline-block;
      overflow: hidden;
      cursor: pointer;
    }

    .image-overlay-container img {
      display: block;
      width: 100%;
      height: auto;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .image-overlay-container:hover img {
      transform: scale(1.05);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }

    .overlay-caption {
      position: absolute;
      bottom: 10px;
      left: 50%;
      transform: translateX(-50%);
      background-color: rgba(0,0,0,0.6);
      color: #fff;
      padding: 5px 10px;
      border-radius: 5px;
      font-weight: bold;
      opacity: 0;
      transition: opacity 0.3s ease;
      pointer-events: none;
    }

    .image-overlay-container:hover .overlay-caption {
      opacity: 1;
    }

    /* Modal */
    .modal {
      display: none;
      position: fixed;
      z-index: 1000;
      left: 0; top: 0;
      width: 100%; height: 100%;
      background-color: rgba(0,0,0,0.6);
    }

    .modal-content {
      background-color: white;
      margin: 10% auto;
      padding: 20px;
      width: 80%;
      max-width: 500px;
      border-radius: 8px;
      position: relative;
    }

    .modal .close {
      position: absolute;
      top: 10px; right: 20px;
      font-size: 24px;
      font-weight: bold;
      cursor: pointer;
    }
  </style>

  {% include header %}

  <!-- Hero and Parallax Intro -->
  <div class="parallax__group reading-width" style="z-index:0;">
    <div class="parallax__layer parallax__layer--back">
      <p class="emphasis"><b>King's College London Engineering Society</b></p>
    </div>
  </div>

  <div class="parallax__group parallax__layer--base bg-dark" style="z-index:1;">
    <div class="parallax__layer parallax__layer--base">
      <div class="reading-width">
        <p class="large">We are the oldest collegiate society of our kind in the world.</p>
        <br />
        <p class="large">Founded in 1847, our society welcomes all students interested in the advancement and future of engineering.</p>
      </div>
    </div>
  </div>

  <div class="section-separator"></div>

  <!-- Stagger Section 1 -->
  <div class="home-content">
    <section class="left">
      <h1>Who We Are</h1>
      <p>We’re a student-led society at King’s College London that unites engineers and innovators from every discipline.</p>
    </section>

    <section class="right-img">
      <div class="image-overlay-container">
        <img src="/static/img/frontpage-1.jpeg" alt="KCL Engineering students" />
        <div class="overlay-caption">MATLAB Onramp Party</div>
      </div>
    </section>
  </div>

  <div class="section-separator"></div>

  <!-- Stagger Section 2 -->
  <div class="home-content">
    <section class="left-img">
      <div class="image-overlay-container">
        <img src="/static/img/frontpage-2.jpeg" alt="KCL Engineering lab" />
        <div class="overlay-caption">Freshers Fair</div>
      </div>
    </section>
    <section class="right">
      <h1>What We Do</h1>
      <p>Our aim is to make engineering accessible to everyone. With close communication to engineering staff at King's, we shape the future of our own education, and gain access to cutting-edge equipment for our learning.</p>
    </section>
  </div>

  <div class="section-separator"></div>

  <!-- Stagger Section 3 -->
  <div class="home-content">
    <section class="left">
      <h1>Upcoming Events</h1>
      <p>We host events throughout the year, ranging from CAD workshops, social meetups, and external engineering speakers. Check out what we have coming up, or sign up for our newsletter.</p>
    </section>

    <section class="right">
      {% assign events = event | futureEvents | sort: "date" %}
      {% for eventItem in events | limit: 3 %}
        <div class="eventItem">
          {% if eventItem.title == "Cross-society networking event: Grow your ecosySTEM" %}
            <h2><a href="https://forms.office.com/Pages/ResponsePage.aspx?id=FM9wg_MWFky4PHJAcWVDVtc9CQa2MPhPjj_5SWwBDOFUNjMySkpNNkJTVko4RktWU1FGV1A4UTU0QS4u" target="_blank">{{ eventItem.title }}</a></h2>
          {% elsif eventItem.title == "MATLAB Workshop" %}
            <h2><a href="javascript:showModal('modal-matlab-workshop')">{{ eventItem.title }}</a></h2>
          {% else %}
            <h2><a href="/events/{{ eventItem.title | slugify }}">{{ eventItem.title }}</a></h2>
          {% endif %}
          <p class="gray">{{ eventItem.date | date: "%A, %d %B %Y" }}</p>
          {{ eventItem.content }}
        </div>
      {% endfor %}
    </section>
  </div>

  <div class="section-separator"></div>
  <hr class="reading-width" />
  <br />

  <!-- Call to Action -->
  <div class="center reading-width">
    <p class="medium"><b>Ready to join us in making a better future?</b></p>
    <a href="https://www.kclsu.org/organisation/11124/">Buy our KCLSU membership and get tickets for KCL X UCL Boat Party</a>
    <br />
    <p>You can also stay connected with us through different platforms:</p>
    <br />
    {% include socials %}
  </div>

  {% include footer %}

  <!-- MATLAB Workshop Modal -->
  <div id="modal-matlab-workshop" class="modal">
    <div class="modal-content">
      <span class="close" onclick="hideModal('modal-matlab-workshop')">&times;</span>
      <h2>MATLAB Workshop</h2>
      <p>Location: To be announced</p>
    </div>
  </div>
</div>

<script>
  // Fade-up stagger on scroll
  const contents = document.querySelectorAll('.home-content');

  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.2 });

  contents.forEach(section => observer.observe(section));

  // Modal show/hide
  function showModal(id) {
    document.getElementById(id).style.display = 'block';
  }

  function hideModal(id) {
    document.getElementById(id).style.display = 'none';
  }
</script>
