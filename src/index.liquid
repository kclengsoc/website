---
layout: home
title: Home
---
<div class="parallax full-width">
	<style>
		header {
			z-index: 2;
			position: relative;
		}

		/* --- Stagger Animations --- */
		.home-content section {
			opacity: 0;
			transform: translateY(40px);
			transition: opacity 0.8s ease-out, transform 0.8s ease-out;
		}

		.home-content.visible section {
			opacity: 1;
			transform: translateY(0);
		}

		.home-content:nth-of-type(odd) section {
			transition-delay: 0.1s;
		}

		.home-content:nth-of-type(even) section {
			transition-delay: 0.3s;
		}

		/* Make stagger smoother */
		@keyframes fadeUp {
			from {
				opacity: 0;
				transform: translateY(30px);
			}
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}
	</style>

	{% include header %}

	<!-- Hero and Parallax Intro (Keep Original) -->
	<div class="parallax__group reading-width" style="z-index:0;">
		<div class="parallax__layer parallax__layer--back">
			<p class="emphasis"><b>King's College London Engineering Society</b></p>
		</div>
	</div>

	<div class="parallax__group parallax__layer--base bg-dark" style="z-index:1;">
		<div class="parallax__layer parallax__layer--base">
			<div class="reading-width">
				<p class="large">We are the oldest collegiate society of
					our kind in the world.</p>
				<br />
				<p class="large">Founded in 1847, our society welcomes all students interested in the advancement and future of
					engineering.</p>
			</div>
		</div>
	</div>

	<div class="section-separator"></div>

	<!-- Stagger Section 1 -->
	<div class="home-content">
		<section class="left">
			<h1>Who We Are</h1>
			<p>We’re a student-led society at King’s College London that unites engineers and innovators from every discipline.</p>
		</section>

		<section class="right-img">
			<img src="/static/img/frontpage-1.jpeg" alt="KCL Engineering students" />
		</section>
	</div>

	<div class="section-separator"></div>

	<!-- Stagger Section 2 -->
	<div class="home-content">
		<section class="left-img">
			<img src="/static/img/frontpage-2.jpeg" alt="KCL Engineering lab" />
		</section>
		<section class="right">
			<h1>What We Do</h1>
			<p>Our aim is to make engineering accessible to everyone. With close communication to engineering staff at
				King's, we shape the future of our own education, and gain access to cutting-edge equipment for our learning.
			</p>
		</section>
	</div>

	<div class="section-separator"></div>

	<!-- Stagger Section 3 -->
	<div class="home-content">
		<section class="left">
			<h1>Upcoming Events</h1>
			<p>We host events throughout the year, ranging from CAD workshops, social meetups, and external
				engineering speakers. Check out what we have coming up, or sign up for our newsletter.</p>
		</section>

		<section class="right">
			{% assign events = event | futureEvents | sort: "date" %}
			{% for eventItem in events | limit: 3 %}
			<div class="eventItem">
				<h2><a href="/events/{{ eventItem.title | slugify }}">{{ eventItem.title }}</a></h2>
				<p class="gray">{{ eventItem.date | date: "%A, %d %B %Y" }}</p>
				{{ eventItem.content }}
			</div>
			{% endfor %}
		</section>
	</div>

	<div class="section-separator"></div>
	<hr class="reading-width" />
	<br />

	<!-- Call to Action -->
	<div class="center reading-width">
		<p class="medium"><b>Ready to join us in making a better future?</b></p>
		<a href="https://www.kclsu.org/organisation/11124/">Buy our KCLSU membership</a>
		<br />
		<p>You can also stay connected with us through different platforms:</p>
		<br />
		{% include socials %}
	</div>

	{% include footer %}
</div>

<script>
	// Fade-up stagger on scroll
	const contents = document.querySelectorAll('.home-content');

	const observer = new IntersectionObserver(entries => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				entry.target.classList.add('visible');
				observer.unobserve(entry.target);
			}
		});
	}, { threshold: 0.2 });

	contents.forEach(section => observer.observe(section));
</script>
